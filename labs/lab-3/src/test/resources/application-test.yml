# Test configuration for lab-3
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

# Default book metadata API URL - will be overridden by WireMockContextInitializer
book:
  metadata:
    api:
      url: https://api.example.com
      timeout: 5